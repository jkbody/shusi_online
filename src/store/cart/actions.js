// import * as type from './type'
// import {request} from '@/util'

const actions = {
  // async [type.SET_PRICE] ({ commit, state }) {
  //   await commit(type.SET_PRICE, {state})
  // }
}
export default actions
